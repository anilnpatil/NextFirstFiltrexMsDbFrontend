import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-view-graphical-report',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './view-graphical-report.html',
  styleUrl: './view-graphical-report.scss',
})
export class ViewGraphicalReport {
  // Component for displaying graphical reports navigation
}
